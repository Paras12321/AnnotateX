"""
resize.py — Image resize logic.

Responsibilities:
    - Resize images only when needed (e.g. very large images) to keep
      inference fast. Does not resize unconditionally.
    - Preserve aspect ratio where possible.

Owner: Member A
"""
