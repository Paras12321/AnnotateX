# sample_data/
# Place test images here for development and demo.
#
# Recommended test set (5-8 small images):
#   - One normal multi-object image (e.g. street scene with people/cars)
#   - One image with no detectable objects (e.g. plain sky or texture)
#   - One image likely to produce low-confidence detections (e.g. distant/blurry objects)
#   - One image with a very small object
#   - One image with overlapping/duplicate-prone objects (e.g. crowd)
#   - One deliberately corrupted file (e.g. truncated JPEG or renamed .txt -> .jpg)
#
# Use small free images (e.g. from COCO validation set or your own photos).
# Do not let dataset sourcing become a blocker — 5-8 images is enough.
